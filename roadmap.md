# Project Roadmap

## Open Tasks

1. **Mobile App Notifications** — Add push notifications so the team knows when important events happen (invoices, orders, payments, etc.).
   - Events: new invoice recorded, new sales request / file uploaded.
   - App type: PWA (installable web app).
   - Blocker: Firebase Cloud Messaging connection was not completed; user will provide Firebase credentials later.

